
import React from 'react';

const TopBar = () => {
  return (
    <div className="sticky top-0 z-40 flex h-16 shrink-0 items-center justify-end gap-4 border-b border-border bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60 px-6">
      {/* Language selector moved to user settings page */}
    </div>
  );
};

export default TopBar;
